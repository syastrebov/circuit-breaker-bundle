framework:
  cache:
    pools:
      circuit_breaker.pool:
        adapter: cache.adapter.filesystem

circuit_breaker:
  provider: memory
  cache_pool: circuit_breaker.pool

  configurations:
    api:
      retries: 2
    default:
      retries: 3
