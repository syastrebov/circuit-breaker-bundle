circuit_breaker:
  provider: predis

  connections:
    redis:
      password: password
      nodes:
        - host: redis-auth-node-1
        - host: redis-auth-node-2
        - host: redis-auth-node-3
          port: 6379

  configurations:
    default:
      retries: 3
      closed_threshold: 10
