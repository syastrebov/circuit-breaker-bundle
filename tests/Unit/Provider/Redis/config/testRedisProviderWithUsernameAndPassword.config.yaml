circuit_breaker:
  provider: redis

  connections:
    redis:
      host: redis-auth
      username: myuser
      password: mypassword

  configurations:
    default:
      retries: 3
      closed_threshold: 10
